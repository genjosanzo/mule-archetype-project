<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans"
	  #foreach($transport in $transports.split(','))
	  http://www.mulesoft.org/schema/mule/${trasport}
	  #end
      #foreach($module in $modules.split(','))http://www.mulesoft.org/schema/mule/${module} 
	  #end
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
		#foreach($transport in $transports.split(','))http://www.mulesoft.org/schema/mule/${transport} http://www.mulesoft.org/schema/mule/${transport}/current/mule-${transport}.xsd
		#end
		#foreach($module in $modules.split(','))http://www.mulesoft.org/schema/mule/${module} http://www.mulesoft.org/schema/mule/${module}/current/mule-${module}.xsd
		#end">

    <description>
    
    </description>

    <flow name="main">
        <vm:inbound-endpoint path="in" exchange-pattern="request-response"/>

        <!-- TODO add your service component here. This can also be a Spring bean using <spring-object bean="name"/> -->
        <echo-component/>

        <vm:outbound-endpoint path="out"/>
    </flow>
</mule>
